apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # Secrets excluded from Flux - managed manually with envsubst to handle template variables
  # - postgres-secret.yaml
  # - auth-secret.yaml
  - helm-repository.yaml
  - helm-release.yaml
  - environment.yaml
  - external-access.yaml
  - security.yaml

# Note: postgres-secret.yaml and auth-secret.yaml are deployed manually
# using scripts/deploy-secrets-with-envsubst.sh to process template variables